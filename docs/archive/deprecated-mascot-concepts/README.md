# Deprecated Mascot Concept Archive

This archive is historical only.

Do not use these files as implementation guidance, source-of-truth product direction, widget direction, mascot naming guidance, asset naming guidance, or future Codex context.

The archived mascot line concepts contain retired names and an obsolete form-evolution model:

- `Cindlet`
- `Voltflare`
- `Moppet`
- `Glisshare`

Current Kairo implementation must use the approved companion identities only:

- `Aetherion`
- `Aurielle`

For current implementation, use native iOS code under `atlas-ios/`, current app assets, active handoff docs, and the current Kairo companion/widget direction. If a future task needs mascot or widget work, treat this directory as explicitly out of bounds unless the user asks to inspect historical context.
